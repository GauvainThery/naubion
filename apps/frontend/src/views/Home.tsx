import React from 'react';
import { HeroSection, MainLayout } from '../components';

const Home: React.FC = () => {
  return (
    <MainLayout>
      <HeroSection />
    </MainLayout>
  );
};

export default Home;
